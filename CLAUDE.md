This project's guidance for coding agents lives in **[AGENTS.md](AGENTS.md)** —
the tool-neutral convention — so there is one copy to keep current. Read that
file before working in this repo.

(A pointer rather than a symlink: git stores symlinks as symlinks, and a clone
on Windows without developer mode turns one into a text file containing the
word "AGENTS.md", which is useless to everybody.)
